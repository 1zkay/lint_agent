[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-alint-agent"
version = "0.1.0"
description = "ALINT-PRO LangGraph Agent Server entrypoint"
requires-python = ">=3.12,<3.13"

[tool.setuptools]
py-modules = [
  "config"
]
packages = [
  "agent_runtime",
  "alint_workflow",
  "alint_workflow.nodes",
  "app",
  "compat",
  "eda",
  "llm",
  "memory",
  "prompts",
  "rag",
  "langgraph_server",
  "mcp_server",
  "mcp_server.tools",
  "workspace",
]
